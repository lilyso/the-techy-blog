<div class="text-center">
  <h2>{{title}}</h2>
  <p>Created by {{user.user_name}} on {{format_date.date_created}}</p>
  <p>{{content}}</p>
  {{#if comments.length}}
    {{#each comments as |comment|}}
      <p>"{{comment.comment}}" - {{comment.user.user_name}}</p>
    {{/each}}
  {{/if}}
</div>